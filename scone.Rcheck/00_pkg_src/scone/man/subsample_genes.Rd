% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sconeReport.R
\name{subsample_genes}
\alias{subsample_genes}
\title{Function to subsample Scone object by subsampling genes}
\usage{
subsample_genes(scone_object, percent = 100, keep_all_control = TRUE,
  seed = 100, verbose = FALSE)
}
\arguments{
\item{scone_object}{a \code{SconeExperiment} object}

\item{percent}{percent of genes you should take if subsampling purely by percent}

\item{keep_all_control}{option to keep all control genes and only
subsample remaining genes to fill to specificed percent}

\item{seed}{the random seed}

\item{verbose}{option to display more information about subsampling}
}
\value{
The subsampled Scone Object
}
\description{
This function subsamples a scone object in a number of different ways by genes
}
\examples{
# Create a scone object
mat <- matrix(rpois(1000, lambda = 5), ncol=10)
colnames(mat) <- paste("X", 1:ncol(mat), sep="")
obj <- SconeExperiment(mat)
res <- scone(obj, scaling=list(none=identity, uq=UQ_FN, deseq=DESEQ_FN),
           evaluate=TRUE, k_ruv=0, k_qc=0, eval_kclust=2,
           bpparam = BiocParallel::SerialParam())
qc = as.matrix(cbind(colSums(mat),colSums(mat > 0)))
rownames(qc) = colnames(mat)
colnames(qc) = c("NCOUNTS","NGENES")

# Subsample the scone object
subsampled_res <- subsample_genes(res, percent=50, keep_all_control = TRUE, seed = 100, verbose = TRUE)


}
